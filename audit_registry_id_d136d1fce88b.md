# Registry ID Audit: agent d136d1fce88b

## ID Generation Logic

All entity IDs (agents, tasks, projects, reviews, etc.) are generated by `_uid()` in
`src/crazypumpkin/framework/models.py:35-36`:

```python
def _uid() -> str:
    return uuid.uuid4().hex[:12]
```

This produces a **12-character lowercase hexadecimal string** — the first 12 hex digits
of a random UUID4. Valid character set: `[0-9a-f]`, length: exactly 12.

## Registration Flow

1. `Agent` dataclass (`models.py:75-83`) has `id: str = field(default_factory=_uid)`.
   When no explicit `id` is passed, `_uid()` auto-generates one.
2. `BaseAgent.__init__` (`agent.py:21`) stores the `Agent` model; `BaseAgent.id` is a
   property delegating to `self.agent.id`.
3. `AgentRegistry.register()` (`registry.py:17-18`) stores the agent in
   `self._agents[agent.id]` — it does **not** generate or validate the ID.
4. There is **no ID format validation** anywhere in the registration path. Any string
   accepted as `Agent.id` will be stored as-is.

## Lookup Logic

- `AgentRegistry.get(agent_id)` — dict lookup by exact string match.
- `AgentRegistry.by_name(name)` — linear scan matching `BaseAgent.name`.
- `AgentRegistry.by_role(role)` — linear scan filtering by role + ACTIVE status.
- `Store` (persistence layer) keys agents/metrics by ID in JSON — also no validation.

## Analysis of d136d1fce88b

| Check                  | Result |
|------------------------|--------|
| Length                  | 12 characters — matches `_uid()` output |
| Character set           | All lowercase hex `[0-9a-f]` — matches `uuid.uuid4().hex` |
| Consistent with _uid()  | **Yes** |

### Conclusion

**`d136d1fce88b` matches the expected ID format** produced by the current `_uid()`
registration flow. It is a valid 12-char hex string consistent with `uuid.uuid4().hex[:12]`.

It is **not** corrupted or legacy-formatted. If this agent ID appears in `state.json`
but not in the live `AgentRegistry`, the most likely explanations are:

1. The agent was registered in a prior process run (IDs are regenerated each startup
   unless explicitly set or loaded from config).
2. The ID was persisted in `state.json` (via `Store.save()`) but the corresponding
   `Agent` object was never re-registered after a restart.
3. The `Agent` was constructed with an explicit `id="d136d1fce88b"` (the dataclass
   allows any string).

The registry is purely in-memory with no persistence — agent entries do not survive
process restarts unless the caller re-registers them.

## Store-Level Persistence (Verified)

`Store.save()` (`store.py:421`) serializes `agent_metrics` keyed by agent ID into
`state.json`. `Store.load()` (`store.py:457`) deserializes them back without validating
that the agent ID corresponds to any currently registered agent. Tasks reference agents
via `assigned_to` (a bare string field) — also no cross-reference validation on load.

This means `d136d1fce88b` can persist indefinitely in `state.json` as an agent_metrics
key or task assignee even after the agent object is gone from the live registry.

## Recommendations

1. **No code change needed** — the ID is valid and the behavior is by design.
2. If phantom agent cleanup is desired, `Store.load()` could optionally cross-check
   `agent_metrics` keys and `task.assigned_to` values against the live `AgentRegistry`.
3. The lack of ID format validation at registration time is low-risk since `_uid()` is
   the only default generator, but an `assert re.fullmatch(r'[0-9a-f]{12}', id)` guard
   in `Agent.__post_init__` would prevent accidental non-hex IDs from entering the system.
